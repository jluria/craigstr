<h1><%= @region.name %></h1>
<div>
<% if current_user.admin = true %>
  <%= link_to "Edit Region", edit_region_path(@region) %>
  <%= link_to "Delete Region", region_path(@region), method: :delete %>
<% end %>
</div>
